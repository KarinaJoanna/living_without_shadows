import 'package:flutter/material.dart';

class TalkAboutDepressionScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Talk About Depression'),
      ),
      body: Center(
        child: Text('Talk About Depression Screen'),
      ),
    );
  }
}
